import chai from 'chai';
import request from 'supertest';
import Server from '../server';

const expect = chai.expect;

describe('Millioanire', () => {
  console.warn(
    'Unfortunately no time for tests, but we are production ready :)'
  );
});
